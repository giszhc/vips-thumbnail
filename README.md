# vips-thumbnail

一个基于 **libvips** 的命令行工具，用来**批量生成图片缩略图**，速度快、占用低，适合处理大量图片。

---

## ✨ 功能特点

- 🚀 批量生成缩略图
- 📁 支持递归处理子目录
- 🖼 支持 JPG / PNG / JPEG
- ⚡ 基于 libvips，性能远好于 ImageMagick
- 🍺 可通过 Homebrew 一行命令安装

---

## 📦 安装方式（推荐）

> ⚠️ 仅支持 macOS（Homebrew）

### 第一步：添加 tap

```bash
brew tap giszhc/vips-thumbnail

```

### 第二步：安装

```
brew install thumbnail
```

安装完成后，会得到一个命令：`thumbnail`

------

## 🚀 使用方法

```
thumbnail <源目录> <输出目录> [参数]
```

### 常用参数说明

| 参数                | 说明                 |
| ------------------- | -------------------- |
| `--size <数字>`     | 缩略图尺寸，默认 400 |
| `--ext <.jpg/.png>` | 输出图片格式（可选） |
| `--recursive`       | 递归处理子目录       |
| `-h, --help`        | 查看帮助             |

------

### 示例

```
thumbnail ./images ./thumbnails --size 300 --recursive
```

含义是：

- 从 `images` 目录读取图片
- 在 `thumbnails` 目录生成缩略图
- 每张图片最大边长 300
- 包含所有子目录

------

## 🔧 依赖说明

- `libvips`
   会由 Homebrew **自动安装**，无需手动处理。

------

## ❓ 常见问题

### Q：为什么这么快？

A：底层使用 `libvips`，这是专业级图像处理库。

### Q：支持 Windows 吗？

A：当前只支持 macOS（Homebrew）。

------

## 📄 License

MIT
